# Submission format and source checks — 2026-09-07

Target: **ACS Sustainable Chemistry & Engineering, Article**. The [official author guidelines](https://researcher-resources.acs.org/publish/author_guidelines?coden=ascecg), updated 2026-08-27, returned HTTP 200 at 2026-09-06 16:05:36 UTC (2026-09-07 in Korea).

The Article limit is 7,000 word equivalents; abstract 150–200 words; keywords 5–8. Sections should be unnumbered, figures embedded, SI separate, and a TOC graphic supplied. Counts exclude the title page, references, captions and TOC; small figures/tables usually contribute 300 words and large ones 600 or more. No independent numerical figure cap was found. AI assistance should be disclosed. These requirements are paraphrased from the checked guidelines.

`manuscript_2026-09-07.md` uses unnumbered sections, six figures from one frozen run, two main tables, separate SI and a programmatic TOC schematic. Its abstract/body counts and both graphic-size assumptions are regenerated in `submission_manuscript_checks_2026-09-07.json`; the conservative estimate assigns 600 words to each analytical figure/table. Final typeset dimensions still govern the journal's count. The TOC does not add a new numerical scientific result.

The [linked TOC specification PDF](https://pubsapp.acs.org/paragonplus/submission/toc_abstract_graphics_guidelines.pdf) timed out in the browsing tool, then returned HTTP 200 through a direct public request. Its specifications were extracted locally: maximum 3.25 × 1.75 inches, sans-serif text at least 6 points, and color TIFF at 300 dpi. The generated companion TIFF is RGB, 975 × 525 pixels at 300 dpi with text at least 7 points; the SVG contains the same programmatic diagram. No source PDF is redistributed. The final DOCX/PDF export should use page numbers and hide source-traceability HTML comments. No submission or paid formatting service was used.

The new draft records AI assistance without inventing author names, affiliations, funding, conflicts, or human approval. Authors must supply those details and a cover letter before submission. This is a coherent scientific submission draft, not an assertion that author approval or editorial acceptance has occurred.

`submission-source-checks-2026-09-07.json` retains current HTTP/identifier outcomes. The Step Method, CatCost and BioSTEAM references matched Crossref; the existing project concept DOI was findable through DataCite and the repository returned HTTP 200. The Nuss–Eckelman Crossref request returned HTTP 429; it was not retried, and its unchanged bibliographic identity relies on the prior `../audit/paper-references-2026-09-06.json` verification. The concept DOI is not represented as a new submission-version deposit. Full publisher texts and third-party databases are not copied into this package.

The SI template-source guard accepts only exact raw, LF or CRLF representations of the manifest hash. This accommodates Git line-ending conversion while keeping every other byte strict. `submission-line-ending-check-2026-09-07.json` records direct assertions against the actual committed `aem_fuel_cell_ccm.json` blob: its LF hash differs from the Windows snapshot, but both LF and CRLF pass; changing the JSON `id` value or appending a space is rejected. No source file was rewritten. Frozen analysis outputs and retained source snapshots still require their exact byte hashes.
